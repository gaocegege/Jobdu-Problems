#include <iostream>

using namespace std;

int min(int a, int b)
{
	return (a > b? b:a);
}

string add (const string &a, const string &b)
{
	int sz = min(a.size(), b.size());
}

int main()
{
	string a, b;
	while (cin >> a)
	{
		cin >> b;

	}
}